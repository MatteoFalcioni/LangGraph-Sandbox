# example.env — rename to .env or pass with --env-file

# --- Core knobs ---
SESSION_STORAGE=TMPFS        # TMPFS | BIND
DATASET_ACCESS=API     # API | LOCAL_RO

# --- Host paths ---
SESSIONS_ROOT=./sessions
BLOBSTORE_DIR=./blobstore
ARTIFACTS_DB=./artifacts.db

# Required ONLY if DATASET_ACCESS=LOCAL_RO
# DATASETS_HOST_RO=./example_llm_data

# --- Docker / runtime ---
SANDBOX_IMAGE=sandbox:latest
TMPFS_SIZE_MB=1024

# --- Artifacts service ---
ARTIFACTS_TOKEN_SECRET=sk-tk123456789098765432112345678900000001111   # any long random string
ARTIFACTS_TOKEN_TTL_SECONDS=600    # 10 minutes; optional (default 600)
ARTIFACTS_PUBLIC_BASE_URL=http://localhost:8000
